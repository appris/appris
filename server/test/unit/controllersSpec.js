/**
 * Created by jmrodriguez on 4/4/14.
 */
'use strict';

/* jasmine specs for controllers go here */

describe('SpeciesCtrl controllers', function() {
    beforeEach(module('apprisApp'));

    describe('SpeciesCtrl', function(){

        it('should create "species" model with 3 species', inject(function($controller) {
            describe('TEST');
            //var scope = {}, ctrl = $controller('SpeciesCtrl', { $scope: scope });
            //expect(scope.species.length).toBe(2);
        }));
    });
});