'use strict';

/* http://docs.angularjs.org/guide/dev_guide.e2e-testing */

describe('apprisApp', function() {

    describe('Specie list view', function() {

        beforeEach(function() {
            browser.get('app/index.html');
        });

//        it('should filter the specie list', function() {
//
//            var specieList = element.all(by.repeater('specie in species'));
//            console.log("specieList"+specieList);
//            var query = element(by.model('query'));
//
////            expect(specieList.count()).toBe(3);
////
////            query.clear();
////            query.sendKeys('nexus');
////            expect(specieList.count()).toBe(1);
////
////            query.clear();
////            query.sendKeys('motorola');
////            expect(specieList.count()).toBe(2);
//        });
    });
});